# 2026-04-29 Initial Release

- packaged the first `capture` skill around the existing troubleshooting transcript prototype
- exposed `dashboard capture.run` as the public DD command
- added temp-file cleanup and safer default capture-file naming
- added Docker verification and coverage proof for transcript and usage behavior

# DD-240

## Title

Restore stdout mirroring for default-output `capture` runs.

## Method

TDD

## Acceptance

- `dashboard capture.run` without `OUTPUT` still prints the command transcript to stdout
- the generated `/tmp` transcript path is still printed after the transcript
- the saved transcript file still matches the printed transcript content
- Docker tests and coverage remain green at 100%
- docs, changelog, commit, and push are completed

## Status

Done

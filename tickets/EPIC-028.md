# EPIC-028

## Title

Package the `capture` troubleshooting transcript skill.

## Goal

Turn the existing `capture` prototype into an isolated DD skill with a public `dashboard capture` command, Docker verification, 100% coverage, and release packaging.

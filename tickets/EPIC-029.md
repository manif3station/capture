# EPIC-029

## Title

Make default `capture` output discoverable.

## Goal

Show the generated `/tmp` transcript path at the end of `dashboard capture.run` when the caller does not provide `OUTPUT`, without changing the explicit-output behavior.
